import React from 'react';
import { Slider } from '../../components';

const SliderDemo = () => (
  <div style={{ textAlign: 'center' }}>
    <Slider duration={2000} random={false} />
  </div>
);
export default SliderDemo;
